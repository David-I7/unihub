Status: ready-for-human

## Parent

.scratch/contribution-view-toggle/PRD.md

## What to build

Update the contribution task forms on the Contribute page to explicitly mark all optional fields with a `(optional)` suffix in the label text. Required fields must not carry this suffix.

## Acceptance criteria

- [ ] All optional fields across catalog and course forms are labeled with the `(optional)` suffix (e.g. `Academic Year ID (optional)`, `Description (optional)`, `startsAt (optional)`).
- [ ] Required fields (e.g. `Course title`, `Material title`, `External URL`) are not labeled as optional.
- [ ] Contribution forms remain responsive and visually appealing.

## Blocked by

None - can start immediately
