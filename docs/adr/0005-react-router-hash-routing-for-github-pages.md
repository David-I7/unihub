# React Router hash routing for GitHub Pages

UniHub will use React Router with hash routing for page navigation. GitHub Pages serves static files and does not receive URL fragments, so hash routes keep direct links and refreshes reliable without requiring a custom fallback redirect setup.
