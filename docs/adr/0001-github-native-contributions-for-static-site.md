# GitHub-native contributions for the static site

UniHub will be deployed on GitHub Pages and will store canonical course data as JSON in the repository, so user contributions will not write directly from the app. The contribution UI will let users choose between creating a GitHub issue containing the proposed JSON or opening a pull request, and maintainers must approve contributions before they become canonical data.
