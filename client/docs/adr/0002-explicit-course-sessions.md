# Explicit course sessions

Course schedules will be stored as explicit course session records instead of recurrence rules. This makes the JSON more verbose, but it avoids ambiguous exceptions for holidays, cancellations, one-off labs, and changed meeting times, which is more important for a static app maintained through reviewed contributions.
