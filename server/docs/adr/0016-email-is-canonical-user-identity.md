# Email is the canonical user identity

UniHub keeps user email globally unique across email/password and Google OAuth2 login. Provider identities link to the same user record so a person can use multiple login methods without creating duplicate accounts.

