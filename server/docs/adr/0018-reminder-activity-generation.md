# Reminder activities are generated per object and schedule

UniHub creates one global reminder activity for each scheduled object and delivery schedule when that reminder becomes due. Email delivery then matches the activity against notification subscriptions, so reminder activities remain global events rather than recipient-specific records.

