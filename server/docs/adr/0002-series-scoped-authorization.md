# Series-scoped authorization

UniHub models `Admin` as a system-wide role and models `Series Representative` and `Contributor` as roles scoped to one or more specific series. This keeps permissions aligned with the domain boundary users actually manage, while allowing one user to represent or contribute to multiple series without receiving global authority.

